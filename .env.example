# Environment Variables Configuration
PORT=3000

# MongoDB Database URI (Leave blank to use built-in local JSON DB fallback, or set your cluster URI)
MONGODB_URI=

# JWT Secret for Session Authentication
JWT_SECRET=super-secret-interview-prep-kit-key-2026

# LLM Provider Configuration (auto, openai, groq, gemini, mock)
LLM_PROVIDER=auto
OPENAI_API_KEY=
GROQ_API_KEY=
GEMINI_API_KEY=
